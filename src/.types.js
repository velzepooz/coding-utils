/**
 * Interfaces declared as type. Use capital "I" for interface and type distinction
 * @typedef {Object} IExample
 * @property {(buf: Buffer) => Promise<void>} write
*/

/**
 * Type example
 * @typedef {'example' | 'type'} Example
*/

/**
 * @callback NumericFunction
 * @param {number} a
 * @returns {number}
*/

// typescript thinks that .js file is a CommonJS file if no export provided
export default {};
